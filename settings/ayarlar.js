require("dotenv").config();

module.exports = {
    TOKEN: process.env.TOKEN || "",  // bot token gir :)
    PREFIX: process.env.PREFIX || "mali", // bot prefix
    SAHİP: process.env.SAHİP || "332180419099361290", //sahip id :()
}
